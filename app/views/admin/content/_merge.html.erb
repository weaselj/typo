<div>
  <h4>Merge Articles</h4>
  <%= form_tag({:controller => 'content',  :action => 'merge', :id => @article.id}) do %>
      <%= label_tag(:merge_with, "Article ID:") %>
      <%= text_field_tag(:merge_with) %>
      <%= submit_tag("Merge") %>
  <% end %>
</div>
